import { CepDirective } from './cep.directive';

describe('CepDirective', () => {
  it('should create an instance', () => {
    const directive = new CepDirective();
    expect(directive).toBeTruthy();
  });
});
