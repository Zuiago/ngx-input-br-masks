import { MoneyDirective } from './money.directive';

describe('MoneyDirective', () => {
  it('should create an instance', () => {
    const directive = new MoneyDirective();
    expect(directive).toBeTruthy();
  });
});
